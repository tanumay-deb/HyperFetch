[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    network: tests that need outbound internet (deselect with -m "not network")
filterwarnings =
    ignore::DeprecationWarning
