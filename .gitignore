# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Build artifacts
build/
dist/
*.zip          # release zips (uploaded to GitHub Releases, not tracked)
*.spec
# ...but keep the hand-maintained PyInstaller specs (canonical build descriptors)
!HyperFetch.spec
!api_server.spec

# Node / extension tests
node_modules/
chrome_ext/test/out.txt
package-lock.json

# Download manager runtime files
*.hfdownload
*.part*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
