"""HyperFetch v2 GUI — clean widget-based rewrite of the View layer.

Backend (queue_manager, downloader, hls, torrent, api_server, utils, task) is
reused unchanged. Launch with `python main.py --v2`.
"""
